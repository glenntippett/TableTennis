<div class="container">
  <div class="row">
    <div class="col">
      <h2>Message Board</h2>
      <% @posts.each do |post| %>
        <h3><%= post.title %></h3>
        <p><%= post.content %></p>
      <% end %>
    </div>
  </div>
</div>
